# TẠI SAO: Không hardcode secrets vào docker-compose.yml
# TẠI SAO: Dễ dàng thay đổi cho từng môi trường (dev/staging/prod)

# Database configuration
DB_ROOT_PASSWORD=SuperSecretRoot123
DB_NAME=fullstack_db
DB_USER=app_user
DB_PASSWORD=AppUserPass456

# Backend configuration
NODE_ENV=production
BACKEND_PORT=5000

# TẠI SAO: .gitignore để không commit lên GitHub